It allows you to clone objects, even complex ones, without coupling to their specific classes.

All prototype classes should have a commom interface that makes it possible to copy objects even if their concrete classes are unknown.